# Cloudflare構成用のcompose上書きです。元のcompose.ymlは変更しません。
# Cloudflare Origin CA証明書とterraform生成のconfをマウントします。mTLSのマウントは行いません。

services:
  nginx:
    volumes:
      - /etc/feedflow/conf.d:/etc/nginx/conf.d:ro
      - /etc/feedflow/tls:/etc/feedflow/tls:ro
